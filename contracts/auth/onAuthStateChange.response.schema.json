{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rynbridge.io/schemas/auth/onAuthStateChange.response.json",
  "title": "AuthOnAuthStateChangeResponse",
  "description": "Event payload for auth.authStateChange event",
  "type": "object",
  "properties": {
    "authenticated": {
      "type": "boolean",
      "description": "Whether the user is currently authenticated"
    },
    "user": {
      "type": ["object", "null"],
      "description": "Authenticated user information, or null if not authenticated",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique user identifier"
        },
        "email": {
          "type": ["string", "null"],
          "description": "User email address, or null if not provided"
        },
        "name": {
          "type": ["string", "null"],
          "description": "User display name, or null if not provided"
        },
        "profileImage": {
          "type": ["string", "null"],
          "description": "URL to user profile image, or null if not provided"
        }
      },
      "required": ["id"],
      "additionalProperties": false
    }
  },
  "required": ["authenticated"],
  "additionalProperties": false
}
