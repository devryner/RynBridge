{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "bluetooth.getState response",
  "type": "object",
  "properties": {
    "state": {
      "type": "string",
      "enum": ["poweredOn", "poweredOff", "unauthorized", "unsupported", "unknown"]
    }
  },
  "required": ["state"]
}
