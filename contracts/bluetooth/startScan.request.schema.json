{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "bluetooth.startScan request",
  "type": "object",
  "properties": {
    "serviceUUIDs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional list of service UUIDs to filter scan results"
    }
  }
}
