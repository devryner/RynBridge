{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rynbridge.io/schemas/contacts/getPermissionStatus.response.json",
  "title": "ContactsGetPermissionStatusResponse",
  "description": "Response payload for contacts.getPermissionStatus action",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["granted", "denied", "notDetermined"],
      "description": "Current contacts permission status"
    }
  },
  "required": ["status"],
  "additionalProperties": false
}
